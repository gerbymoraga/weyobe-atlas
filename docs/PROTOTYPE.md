# UX prototype

Source of truth for layout, states, and brand: [`atlas-portal.html`](./atlas-portal.html).

Open in a browser to click through member flows. Flip `MEMBER_TIER` between `"navigator"` and `"expedition"` in the file to compare tier states.
